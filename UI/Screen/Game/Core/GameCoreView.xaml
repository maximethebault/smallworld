<UserControl x:Class="UI.Screen.Game.Core.GameCoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:UI"
             xmlns:core="clr-namespace:UI.Screen.Game.Core"
             xmlns:map="clr-namespace:UI.Screen.Game.Core.Map"
             xmlns:unit="clr-namespace:UI.Screen.Game.Core.Unit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="GameCoreControl"
             d:DataContext="{d:DesignInstance core:GameCoreViewModel}">
    <DockPanel>
        <Grid Name="MapGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ContentControl x:Name="Map" Grid.Column="0" DataContext="{Binding Path=MapViewModel}">
                <map:MapView />
            </ContentControl>
            <ContentControl x:Name="Unit" Grid.Column="1" DataContext="{Binding Path=UnitsViewModel}">
                <unit:UnitsView />
            </ContentControl>
        </Grid>
    </DockPanel>
</UserControl>
